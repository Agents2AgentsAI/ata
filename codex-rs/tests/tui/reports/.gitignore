# Per-run agentic test reports and verdicts are local artifacts (generated by
# run-agentic.sh / the driver agent), not source. Keep the directory via
# .gitkeep but never track the generated reports.
*
!.gitkeep
!.gitignore
