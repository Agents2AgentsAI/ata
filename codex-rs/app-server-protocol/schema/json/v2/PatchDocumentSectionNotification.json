{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "definitions": {
    "PatchDocumentSectionEvent": {
      "description": "Event emitted when the agent calls `patch_document_section`.",
      "properties": {
        "call_id": {
          "type": "string"
        },
        "document_id": {
          "type": "string"
        },
        "foldable": {
          "default": false,
          "description": "Whether the patched content should be collapsible.",
          "type": "boolean"
        },
        "new_text": {
          "type": "string"
        },
        "old_text": {
          "type": "string"
        },
        "section_index": {
          "format": "uint",
          "minimum": 0.0,
          "type": "integer"
        },
        "summary": {
          "default": null,
          "description": "Short summary shown when the fold is collapsed.",
          "type": [
            "string",
            "null"
          ]
        },
        "turn_id": {
          "type": "string"
        }
      },
      "required": [
        "call_id",
        "document_id",
        "new_text",
        "old_text",
        "section_index",
        "turn_id"
      ],
      "type": "object"
    }
  },
  "properties": {
    "event": {
      "$ref": "#/definitions/PatchDocumentSectionEvent"
    },
    "threadId": {
      "type": "string"
    }
  },
  "required": [
    "event",
    "threadId"
  ],
  "title": "PatchDocumentSectionNotification",
  "type": "object"
}