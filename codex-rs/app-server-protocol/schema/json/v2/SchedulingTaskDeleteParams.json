{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "definitions": {
    "SchedulingTaskKind": {
      "enum": [
        "cron",
        "monitor"
      ],
      "type": "string"
    }
  },
  "description": "ATA: delete one scheduling task (cron job, monitor, or loop) from a thread's registry. Used by the `/scheduling` TUI panel's `d` shortcut. Server aborts the task first if it's still running. Response is empty; the panel observes the removal via the next `SchedulingTasksSnapshot` notification, which the server emits immediately after the delete.",
  "properties": {
    "kind": {
      "$ref": "#/definitions/SchedulingTaskKind"
    },
    "taskId": {
      "type": "string"
    },
    "threadId": {
      "type": "string"
    }
  },
  "required": [
    "kind",
    "taskId",
    "threadId"
  ],
  "title": "SchedulingTaskDeleteParams",
  "type": "object"
}