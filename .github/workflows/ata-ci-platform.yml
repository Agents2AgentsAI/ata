name: ata-ci-platform

# ATA-specific platform CI using RunsOn runners.
# Kept separate from upstream rust-ci.yml / rust-ci-full.yml to avoid merge conflicts.

on:
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review]
    paths:
      - "codex-rs/**"
      - ".github/workflows/**"
      - ".github/actions/**"
      - ".github/scripts/**"
  push:
    branches: [main]
    paths:
      - "codex-rs/**"
      - ".github/workflows/**"
      - ".github/actions/**"
      - ".github/scripts/**"
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: ${{ github.ref_name != 'main' }}

# CI builds in debug (dev) for faster signal.

jobs:
  # --- Detect what changed ---------------------------------------------------
  changed:
    name: Detect changed areas
    runs-on: ubuntu-24.04
    outputs:
      codex: ${{ steps.detect.outputs.codex }}
      workflows: ${{ steps.detect.outputs.workflows }}
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - name: Detect changed paths (no external action)
        id: detect
        shell: bash
        run: |
          set -euo pipefail

          if [[ "${{ github.event_name }}" == "pull_request" ]]; then
            BASE_SHA='${{ github.event.pull_request.base.sha }}'
            HEAD_SHA='${{ github.event.pull_request.head.sha }}'
            echo "Base SHA: $BASE_SHA"
            echo "Head SHA: $HEAD_SHA"
            mapfile -t files < <(git diff --name-only --no-renames "$BASE_SHA" "$HEAD_SHA")
          else
            # On push / manual runs, default to running everything
            files=("codex-rs/force" ".github/force")
          fi

          codex=false
          workflows=false
          for f in "${files[@]}"; do
            [[ $f == codex-rs/* ]] && codex=true
            [[ $f == .github/* ]] && workflows=true
          done

          echo "codex=$codex" >> "$GITHUB_OUTPUT"
          echo "workflows=$workflows" >> "$GITHUB_OUTPUT"

  # --- CI to validate on different os/targets --------------------------------
  lint_build:
    name: Lint/Build — ${{ matrix.runner }} - ${{ matrix.target }}${{ matrix.profile == 'release' && ' (release)' || '' }}
    runs-on: ${{ matrix.runs_on || matrix.runner }}
    timeout-minutes: 45
    needs: changed
    # Skip expensive matrix jobs on draft PRs and when no relevant files changed.
    if: ${{ !github.event.pull_request.draft && (needs.changed.outputs.codex == 'true' || needs.changed.outputs.workflows == 'true' || github.event_name == 'push') }}
    defaults:
      run:
        working-directory: codex-rs
    env:
      # Speed up repeated builds across CI runs by caching compiled objects (non-Windows).
      USE_SCCACHE: ${{ startsWith(matrix.runner, 'windows') && 'false' || 'true' }}
      CARGO_INCREMENTAL: "0"
      SCCACHE_CACHE_SIZE: 10G
      # In rust-ci, representative release-profile checks use thin LTO for faster feedback.
      CARGO_PROFILE_RELEASE_LTO: ${{ matrix.profile == 'release' && 'thin' || 'fat' }}

    strategy:
      fail-fast: false
      matrix:
        include:
          # macOS — GitHub-hosted (no RunsOn macOS support).
          - runner: macos-15-xlarge
            target: aarch64-apple-darwin
            profile: dev
          - runner: macos-15-xlarge
            target: x86_64-apple-darwin
            profile: dev
          # Linux — RunsOn w/ S3 magic cache.
          - runner: ubuntu-x64
            target: x86_64-unknown-linux-musl
            profile: dev
            runs_on: "runs-on=${{ github.run_id }}/runner=ci-linux-x64/extras=s3-cache/volume=160gb/spot=false"
          - runner: ubuntu-x64
            target: x86_64-unknown-linux-gnu
            profile: dev
            runs_on: "runs-on=${{ github.run_id }}/runner=ci-linux-x64/extras=s3-cache/volume=160gb/spot=false"
          - runner: ubuntu-arm64
            target: aarch64-unknown-linux-musl
            profile: dev
            runs_on: "runs-on=${{ github.run_id }}/runner=ci-linux-arm64/extras=s3-cache/volume=160gb/spot=false"
          - runner: ubuntu-arm64
            target: aarch64-unknown-linux-gnu
            profile: dev
            runs_on: "runs-on=${{ github.run_id }}/runner=ci-linux-arm64/extras=s3-cache/volume=160gb/spot=false"
          # Windows — RunsOn (no magic cache on Windows).
          - runner: windows-x64
            target: x86_64-pc-windows-msvc
            profile: dev
            runs_on: "runs-on=${{ github.run_id }}/runner=ci-windows-x64/volume=160gb"

          # Also run representative release builds on Mac and Linux because
          # there could be release-only build errors we want to catch.
          # Hopefully this also pre-populates the build cache to speed up
          # releases.
          - runner: macos-15-xlarge
            target: aarch64-apple-darwin
            profile: release
          - runner: ubuntu-x64
            target: x86_64-unknown-linux-musl
            profile: release
            runs_on: "runs-on=${{ github.run_id }}/runner=ci-linux-x64/extras=s3-cache/volume=160gb/spot=false"
          - runner: ubuntu-arm64
            target: aarch64-unknown-linux-musl
            profile: release
            runs_on: "runs-on=${{ github.run_id }}/runner=ci-linux-arm64/extras=s3-cache/volume=160gb/spot=false"
          - runner: windows-x64
            target: x86_64-pc-windows-msvc
            profile: release
            runs_on: "runs-on=${{ github.run_id }}/runner=ci-windows-x64/volume=160gb"

    steps:
      - uses: actions/checkout@v6
      - name: Activate RunsOn
        if: ${{ contains(matrix.runs_on || '', 'runs-on=') }}
        uses: runs-on/action@v2
      - name: Set up MSVC dev environment
        if: ${{ runner.os == 'Windows' }}
        uses: ilammy/msvc-dev-cmd@v1
      - name: Install Linux build dependencies
        if: ${{ runner.os == 'Linux' }}
        shell: bash
        run: |
          set -euo pipefail
          if command -v apt-get >/dev/null 2>&1; then
            sudo apt-get update -y
            # libasound2-dev is required by `alsa-sys` (transitively pulled in by
            # `cpal`) when codex-tui builds the voice/audio_device modules.
            # libc++-20-dev / libc++abi-20-dev provide the newer libcxx ABI the
            # prebuilt v8 `ptrcomp_sandbox_release` archive references (e.g.
            # std::__1::__hash_memory). Fall back to the default 18 if 20 isn't
            # available on this runner.
            packages=(pkg-config libcap-dev libasound2-dev)
            if [[ "${{ matrix.target }}" == 'x86_64-unknown-linux-musl' || "${{ matrix.target }}" == 'aarch64-unknown-linux-musl' ]]; then
              packages+=(libubsan1)
            fi
            if sudo DEBIAN_FRONTEND=noninteractive apt-get install -y --no-install-recommends "${packages[@]}" libc++-20-dev libc++abi-20-dev; then
              llvm_lib_dir=""
              for candidate in /usr/lib/llvm-20/lib/x86_64-unknown-linux-gnu \
                               /usr/lib/llvm-20/lib/aarch64-unknown-linux-gnu \
                               /usr/lib/llvm-20/lib; do
                if [ -e "$candidate/libc++.so" ] || [ -e "$candidate/libc++.so.1" ]; then
                  llvm_lib_dir="$candidate"; break
                fi
              done
              if [ -n "$llvm_lib_dir" ]; then
                echo "LIBRARY_PATH=${llvm_lib_dir}" >> "$GITHUB_ENV"
                echo "LD_LIBRARY_PATH=${llvm_lib_dir}" >> "$GITHUB_ENV"
                echo "Selected libc++ dir: ${llvm_lib_dir}"
              fi
            else
              sudo DEBIAN_FRONTEND=noninteractive apt-get install -y --no-install-recommends "${packages[@]}" libc++-dev libc++abi-dev
            fi
          fi
      - uses: dtolnay/rust-toolchain@1.93.0
        with:
          targets: ${{ matrix.target }}
          components: clippy

      - if: ${{ matrix.target == 'x86_64-unknown-linux-musl' || matrix.target == 'aarch64-unknown-linux-musl'}}
        name: Use hermetic Cargo home (musl)
        shell: bash
        run: |
          set -euo pipefail
          cargo_home="${GITHUB_WORKSPACE}/.cargo-home"
          mkdir -p "${cargo_home}/bin"
          echo "CARGO_HOME=${cargo_home}" >> "$GITHUB_ENV"
          echo "${cargo_home}/bin" >> "$GITHUB_PATH"
          : > "${cargo_home}/config.toml"

      - name: Compute lockfile hash
        id: lockhash
        working-directory: codex-rs
        shell: bash
        run: |
          set -euo pipefail
          echo "hash=$(sha256sum Cargo.lock | cut -d' ' -f1)" >> "$GITHUB_OUTPUT"
          echo "toolchain_hash=$(sha256sum rust-toolchain.toml | cut -d' ' -f1)" >> "$GITHUB_OUTPUT"

      # Explicit cache restore: split cargo home vs target, so we can
      # avoid caching the large target dir on the gnu-dev job.
      - name: Restore cargo home cache
        id: cache_cargo_home_restore
        uses: actions/cache/restore@v5
        with:
          path: |
            ~/.cargo/bin/
            ~/.cargo/registry/index/
            ~/.cargo/registry/cache/
            ~/.cargo/git/db/
            ${{ github.workspace }}/.cargo-home/bin/
            ${{ github.workspace }}/.cargo-home/registry/index/
            ${{ github.workspace }}/.cargo-home/registry/cache/
            ${{ github.workspace }}/.cargo-home/git/db/
          key: cargo-home-${{ matrix.runner }}-${{ matrix.target }}-${{ matrix.profile }}-${{ steps.lockhash.outputs.hash }}-${{ steps.lockhash.outputs.toolchain_hash }}
          restore-keys: |
            cargo-home-${{ matrix.runner }}-${{ matrix.target }}-${{ matrix.profile }}-

      # Install and restore sccache cache
      - name: Install sccache
        if: ${{ env.USE_SCCACHE == 'true' }}
        uses: taiki-e/install-action@44c6d64aa62cd779e873306675c7a58e86d6d532 # v2
        with:
          tool: sccache
          version: 0.7.5

      - name: Configure sccache backend
        if: ${{ env.USE_SCCACHE == 'true' }}
        shell: bash
        run: |
          set -euo pipefail
          if [[ -n "${ACTIONS_CACHE_URL:-}" && -n "${ACTIONS_RUNTIME_TOKEN:-}" ]]; then
            echo "SCCACHE_GHA_ENABLED=true" >> "$GITHUB_ENV"
            echo "Using sccache GitHub backend"
          else
            echo "SCCACHE_GHA_ENABLED=false" >> "$GITHUB_ENV"
            echo "SCCACHE_DIR=${{ github.workspace }}/.sccache" >> "$GITHUB_ENV"
            echo "Using sccache local disk + actions/cache fallback"
          fi

      - name: Enable sccache wrapper
        if: ${{ env.USE_SCCACHE == 'true' }}
        shell: bash
        run: echo "RUSTC_WRAPPER=sccache" >> "$GITHUB_ENV"

      - name: Restore sccache cache (fallback)
        if: ${{ env.USE_SCCACHE == 'true' && env.SCCACHE_GHA_ENABLED != 'true' }}
        id: cache_sccache_restore
        uses: actions/cache/restore@v5
        with:
          path: ${{ github.workspace }}/.sccache/
          key: sccache-${{ matrix.runner }}-${{ matrix.target }}-${{ matrix.profile }}-${{ steps.lockhash.outputs.hash }}-${{ github.run_id }}
          restore-keys: |
            sccache-${{ matrix.runner }}-${{ matrix.target }}-${{ matrix.profile }}-${{ steps.lockhash.outputs.hash }}-
            sccache-${{ matrix.runner }}-${{ matrix.target }}-${{ matrix.profile }}-

      - if: ${{ matrix.target == 'x86_64-unknown-linux-musl' || matrix.target == 'aarch64-unknown-linux-musl'}}
        name: Disable sccache wrapper (musl)
        shell: bash
        run: |
          set -euo pipefail
          echo "RUSTC_WRAPPER=" >> "$GITHUB_ENV"
          echo "RUSTC_WORKSPACE_WRAPPER=" >> "$GITHUB_ENV"

      - if: ${{ matrix.target == 'x86_64-unknown-linux-musl' || matrix.target == 'aarch64-unknown-linux-musl'}}
        name: Prepare APT cache directories (musl)
        shell: bash
        run: |
          set -euo pipefail
          sudo mkdir -p /var/cache/apt/archives /var/lib/apt/lists
          sudo chown -R "$USER:$USER" /var/cache/apt /var/lib/apt/lists

      - if: ${{ matrix.target == 'x86_64-unknown-linux-musl' || matrix.target == 'aarch64-unknown-linux-musl'}}
        name: Restore APT cache (musl)
        id: cache_apt_restore
        uses: actions/cache/restore@v5
        with:
          path: |
            /var/cache/apt
          key: apt-${{ matrix.runner }}-${{ matrix.target }}-v1

      - if: ${{ matrix.target == 'x86_64-unknown-linux-musl' || matrix.target == 'aarch64-unknown-linux-musl'}}
        name: Install Zig
        uses: mlugg/setup-zig@v2
        with:
          version: 0.14.1

      - if: ${{ matrix.target == 'x86_64-unknown-linux-musl' || matrix.target == 'aarch64-unknown-linux-musl'}}
        name: Install musl build tools
        env:
          DEBIAN_FRONTEND: noninteractive
          TARGET: ${{ matrix.target }}
          APT_UPDATE_ARGS: -o Acquire::Retries=3
          APT_INSTALL_ARGS: --no-install-recommends
        shell: bash
        run: bash "${GITHUB_WORKSPACE}/.github/scripts/install-musl-build-tools.sh"

      - if: ${{ matrix.target == 'x86_64-unknown-linux-musl' || matrix.target == 'aarch64-unknown-linux-musl'}}
        name: Configure rustc UBSan wrapper (musl host)
        shell: bash
        run: |
          set -euo pipefail
          ubsan=""
          if command -v ldconfig >/dev/null 2>&1; then
            ubsan="$(ldconfig -p | grep -m1 'libubsan\.so\.1' | sed -E 's/.*=> (.*)$/\1/')"
          fi
          wrapper_root="${RUNNER_TEMP:-/tmp}"
          wrapper="${wrapper_root}/rustc-ubsan-wrapper"
          cat > "${wrapper}" <<EOF
          #!/usr/bin/env bash
          set -euo pipefail
          if [[ -n "${ubsan}" ]]; then
            export LD_PRELOAD="${ubsan}\${LD_PRELOAD:+:\${LD_PRELOAD}}"
          fi
          exec "\$1" "\${@:2}"
          EOF
          chmod +x "${wrapper}"
          echo "RUSTC_WRAPPER=${wrapper}" >> "$GITHUB_ENV"
          echo "RUSTC_WORKSPACE_WRAPPER=" >> "$GITHUB_ENV"

      - if: ${{ matrix.target == 'x86_64-unknown-linux-musl' || matrix.target == 'aarch64-unknown-linux-musl'}}
        name: Clear sanitizer flags (musl)
        shell: bash
        run: |
          set -euo pipefail
          # Clear global Rust flags so host/proc-macro builds don't pull in UBSan.
          echo "RUSTFLAGS=" >> "$GITHUB_ENV"
          echo "CARGO_ENCODED_RUSTFLAGS=" >> "$GITHUB_ENV"
          echo "RUSTDOCFLAGS=" >> "$GITHUB_ENV"
          # Override any runner-level Cargo config rustflags as well.
          echo "CARGO_BUILD_RUSTFLAGS=" >> "$GITHUB_ENV"
          echo "CARGO_TARGET_X86_64_UNKNOWN_LINUX_GNU_RUSTFLAGS=" >> "$GITHUB_ENV"
          echo "CARGO_TARGET_AARCH64_UNKNOWN_LINUX_GNU_RUSTFLAGS=" >> "$GITHUB_ENV"
          echo "CARGO_TARGET_X86_64_UNKNOWN_LINUX_MUSL_RUSTFLAGS=" >> "$GITHUB_ENV"
          echo "CARGO_TARGET_AARCH64_UNKNOWN_LINUX_MUSL_RUSTFLAGS=" >> "$GITHUB_ENV"

          sanitize_flags() {
            local input="$1"
            input="${input//-fsanitize=undefined/}"
            input="${input//-fno-sanitize-recover=undefined/}"
            input="${input//-fno-sanitize-trap=undefined/}"
            echo "$input"
          }

          cflags="$(sanitize_flags "${CFLAGS-}")"
          cxxflags="$(sanitize_flags "${CXXFLAGS-}")"
          echo "CFLAGS=${cflags}" >> "$GITHUB_ENV"
          echo "CXXFLAGS=${cxxflags}" >> "$GITHUB_ENV"

      - name: Configure rusty_v8 artifact overrides
        # v8 v147+ defaults to the `ptrcomp_sandbox` build variant, but the
        # upstream `denoland/rusty_v8` releases only ship the
        # `release` / `ptrcomp_release` / `simdutf_release` variants and
        # NOT `ptrcomp_sandbox` for any platform. The upstream Codex release
        # publishes the missing `ptrcomp_sandbox` artifacts (along with the
        # plain `release` musl ones), so point both `RUSTY_V8_ARCHIVE` and
        # `RUSTY_V8_SRC_BINDING_PATH` at the upstream release for every
        # target. Without this override the v8 build script gets HTTP 404
        # from denoland and the whole Lint/Build matrix fails.
        # `GN_ARGS=use_custom_libcxx=false` makes the v8 build script emit
        # the `cargo:rustc-link-lib=stdc++` (linux) / `c++` (mac/bsd)
        # directive, since the prebuilt ptrcomp_sandbox archives do not bundle
        # libcxx and would otherwise leave libv8 with unresolved C++ symbols.
        env:
          TARGET: ${{ matrix.target }}
        shell: bash
        run: |
          set -euo pipefail
          version="$(python3 "${GITHUB_WORKSPACE}/.github/scripts/rusty_v8_bazel.py" resolved-v8-crate-version)"
          release_tag="rusty-v8-v${version}"
          base_url="https://github.com/openai/codex/releases/download/${release_tag}"
          archive_name="librusty_v8_ptrcomp_sandbox_release_${TARGET}.a.gz"
          binding_name="src_binding_ptrcomp_sandbox_release_${TARGET}.rs"
          archive="${base_url}/${archive_name}"
          binding_dir="${RUNNER_TEMP}/rusty_v8"
          binding_path="${binding_dir}/${binding_name}"
          mkdir -p "${binding_dir}"
          curl -fsSL "${base_url}/${binding_name}" -o "${binding_path}"
          echo "RUSTY_V8_ARCHIVE=${archive}" >> "$GITHUB_ENV"
          echo "RUSTY_V8_SRC_BINDING_PATH=${binding_path}" >> "$GITHUB_ENV"
          echo "GN_ARGS=use_custom_libcxx=false" >> "$GITHUB_ENV"
          # The upstream Codex release `ptrcomp_sandbox_release` archive is built against
          # libc++ (`std::__1::*` symbols) on every platform, so route the v8
          # build script's link directive to libc++ instead of the per-target
          # default (libstdc++ on linux-gnu).
          case "${TARGET}" in
            *-linux-gnu|*-linux-musl) echo "CXXSTDLIB=c++" >> "$GITHUB_ENV" ;;
          esac

      - if: ${{ contains(matrix.target, 'aarch64-unknown-linux') }}
        name: Cap build parallelism (ARM)
        shell: bash
        run: echo "CARGO_BUILD_JOBS=4" >> "$GITHUB_ENV"

      - name: Install cargo-chef
        if: ${{ matrix.profile == 'release' }}
        uses: taiki-e/install-action@44c6d64aa62cd779e873306675c7a58e86d6d532 # v2
        with:
          tool: cargo-chef
          version: 0.1.71

      - name: Pre-warm dependency cache (cargo-chef)
        if: ${{ matrix.profile == 'release' }}
        shell: bash
        # NOTE: cargo-chef replaces each workspace crate's source with an
        # empty stub in place, builds the registry/dep cache, and leaves the
        # stubs on disk. In the Docker pattern this is fine because the next
        # stage does `COPY . .` to restore real source on top. In this CI job
        # there is no such restore, so follow-up steps (clippy, tests) would
        # see the stubbed source. Cook without `--all-features` so we don't
        # pull in optional path deps that try to compile against the emptied
        # codex-rs sources.
        #   * We manually restore source with `git checkout` after cook so
        #     the subsequent `cargo clippy` step sees the real files.
        run: |
          set -euo pipefail
          RECIPE="${RUNNER_TEMP}/chef-recipe.json"
          cargo chef prepare --recipe-path "$RECIPE"
          cargo chef cook --recipe-path "$RECIPE" --target ${{ matrix.target }} --release

      - name: Restore workspace source after cargo-chef cook
        if: ${{ matrix.profile == 'release' }}
        shell: bash
        working-directory: ${{ github.workspace }}
        # cargo-chef leaves every workspace crate's src replaced with an empty
        # stub; restore the real source before the real clippy/test runs.
        run: git checkout -- codex-rs

      - name: cargo clippy
        run: cargo clippy --target ${{ matrix.target }} --all-features --tests --profile ${{ matrix.profile }} --timings -- -D warnings

      - name: Upload Cargo timings (clippy)
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: cargo-timings-ata-clippy-${{ matrix.target }}-${{ matrix.profile }}
          path: codex-rs/target/**/cargo-timings/cargo-timing.html
          if-no-files-found: warn

      # Save caches explicitly; make non-fatal so cache packaging
      # never fails the overall job. Only save when key wasn't hit.
      - name: Save cargo home cache
        if: always() && !cancelled() && steps.cache_cargo_home_restore.outputs.cache-hit != 'true'
        continue-on-error: true
        uses: actions/cache/save@v5
        with:
          path: |
            ~/.cargo/bin/
            ~/.cargo/registry/index/
            ~/.cargo/registry/cache/
            ~/.cargo/git/db/
            ${{ github.workspace }}/.cargo-home/bin/
            ${{ github.workspace }}/.cargo-home/registry/index/
            ${{ github.workspace }}/.cargo-home/registry/cache/
            ${{ github.workspace }}/.cargo-home/git/db/
          key: cargo-home-${{ matrix.runner }}-${{ matrix.target }}-${{ matrix.profile }}-${{ steps.lockhash.outputs.hash }}-${{ steps.lockhash.outputs.toolchain_hash }}

      - name: Save sccache cache (fallback)
        if: always() && !cancelled() && env.USE_SCCACHE == 'true' && env.SCCACHE_GHA_ENABLED != 'true'
        continue-on-error: true
        uses: actions/cache/save@v5
        with:
          path: ${{ github.workspace }}/.sccache/
          key: sccache-${{ matrix.runner }}-${{ matrix.target }}-${{ matrix.profile }}-${{ steps.lockhash.outputs.hash }}-${{ github.run_id }}

      - name: sccache stats
        if: always() && env.USE_SCCACHE == 'true'
        continue-on-error: true
        working-directory: .
        run: sccache --show-stats || true

      - name: sccache summary
        if: always() && env.USE_SCCACHE == 'true'
        shell: bash
        working-directory: .
        run: |
          {
            echo "### sccache stats — ${{ matrix.target }} (${{ matrix.profile }})";
            echo;
            echo '```';
            sccache --show-stats || true;
            echo '```';
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Save APT cache (musl)
        if: always() && !cancelled() && (matrix.target == 'x86_64-unknown-linux-musl' || matrix.target == 'aarch64-unknown-linux-musl') && steps.cache_apt_restore.outputs.cache-hit != 'true'
        continue-on-error: true
        uses: actions/cache/save@v5
        with:
          path: |
            /var/cache/apt
          key: apt-${{ matrix.runner }}-${{ matrix.target }}-v1

  tests:
    name: Tests — ${{ matrix.runner }} - ${{ matrix.target }}
    runs-on: ${{ matrix.runs_on || matrix.runner }}
    timeout-minutes: 45
    needs: changed
    if: ${{ !github.event.pull_request.draft && (needs.changed.outputs.codex == 'true' || needs.changed.outputs.workflows == 'true' || github.event_name == 'push') }}
    defaults:
      run:
        working-directory: codex-rs
    env:
      # Speed up repeated builds across CI runs by caching compiled objects (non-Windows).
      USE_SCCACHE: ${{ startsWith(matrix.runner, 'windows') && 'false' || 'true' }}
      CARGO_INCREMENTAL: "0"
      SCCACHE_CACHE_SIZE: 10G
      # Windows default thread stack is 1 MB; rustc needs more for large --all-features test builds.
      RUST_MIN_STACK: "16777216"

    strategy:
      fail-fast: false
      matrix:
        include:
          - runner: macos-15-xlarge
            target: aarch64-apple-darwin
            profile: dev
          - runner: ubuntu-x64
            target: x86_64-unknown-linux-gnu
            profile: dev
            runs_on: "runs-on=${{ github.run_id }}/runner=ci-linux-x64/extras=s3-cache/volume=160gb/spot=false"
          - runner: ubuntu-arm64
            target: aarch64-unknown-linux-gnu
            profile: dev
            runs_on: "runs-on=${{ github.run_id }}/runner=ci-linux-arm64/extras=s3-cache/volume=160gb/spot=false"
          - runner: windows-x64
            target: x86_64-pc-windows-msvc
            profile: dev
            runs_on: "runs-on=${{ github.run_id }}/runner=ci-windows-x64/volume=160gb"

    steps:
      - uses: actions/checkout@v6
      - name: Activate RunsOn
        if: ${{ contains(matrix.runs_on || '', 'runs-on=') }}
        uses: runs-on/action@v2
      - name: Set up MSVC dev environment
        if: ${{ runner.os == 'Windows' }}
        uses: ilammy/msvc-dev-cmd@v1
      - name: Set up Node.js for js_repl tests
        uses: actions/setup-node@v6
        with:
          node-version-file: codex-rs/node-version.txt
      - name: Install Linux build dependencies
        if: ${{ runner.os == 'Linux' }}
        shell: bash
        run: |
          set -euo pipefail
          if command -v apt-get >/dev/null 2>&1; then
            sudo apt-get update -y
            # libc++-20-dev / libc++abi-20-dev are needed because the v8 prebuilt
            # was built against newer libc++ and references symbols (e.g.
            # std::__1::__hash_memory) not present in Ubuntu noble's default
            # libc++-18.
            if sudo DEBIAN_FRONTEND=noninteractive apt-get install -y --no-install-recommends \
                pkg-config libcap-dev libasound2-dev bubblewrap \
                libc++-20-dev libc++abi-20-dev; then
              # libc++-20 ships under /usr/lib/llvm-20/lib (or the per-arch
              # subdir). Put it first on LIBRARY_PATH so `-lc++` picks the
              # newer libc++ over the system default libc++-18.
              llvm_lib_dir=""
              for candidate in /usr/lib/llvm-20/lib/x86_64-unknown-linux-gnu \
                               /usr/lib/llvm-20/lib/aarch64-unknown-linux-gnu \
                               /usr/lib/llvm-20/lib; do
                if [ -e "$candidate/libc++.so" ] || [ -e "$candidate/libc++.so.1" ]; then
                  llvm_lib_dir="$candidate"; break
                fi
              done
              if [ -n "$llvm_lib_dir" ]; then
                echo "LIBRARY_PATH=${llvm_lib_dir}" >> "$GITHUB_ENV"
                echo "LD_LIBRARY_PATH=${llvm_lib_dir}" >> "$GITHUB_ENV"
                echo "Selected libc++ dir: ${llvm_lib_dir}"
              fi
            else
              sudo DEBIAN_FRONTEND=noninteractive apt-get install -y --no-install-recommends \
                pkg-config libcap-dev libasound2-dev bubblewrap libc++-dev libc++abi-dev
            fi
          fi
      # Some integration tests rely on DotSlash being installed.
      - name: Install DotSlash
        uses: facebook/install-dotslash@v2

      - uses: dtolnay/rust-toolchain@1.93.0
        with:
          targets: ${{ matrix.target }}

      - name: Compute lockfile hash
        id: lockhash
        working-directory: codex-rs
        shell: bash
        run: |
          set -euo pipefail
          echo "hash=$(sha256sum Cargo.lock | cut -d' ' -f1)" >> "$GITHUB_OUTPUT"
          echo "toolchain_hash=$(sha256sum rust-toolchain.toml | cut -d' ' -f1)" >> "$GITHUB_OUTPUT"

      - name: Restore cargo home cache
        id: cache_cargo_home_restore
        uses: actions/cache/restore@v5
        with:
          path: |
            ~/.cargo/bin/
            ~/.cargo/registry/index/
            ~/.cargo/registry/cache/
            ~/.cargo/git/db/
          key: cargo-home-${{ matrix.runner }}-${{ matrix.target }}-${{ matrix.profile }}-${{ steps.lockhash.outputs.hash }}-${{ steps.lockhash.outputs.toolchain_hash }}
          restore-keys: |
            cargo-home-${{ matrix.runner }}-${{ matrix.target }}-${{ matrix.profile }}-

      - name: Install sccache
        if: ${{ env.USE_SCCACHE == 'true' }}
        uses: taiki-e/install-action@44c6d64aa62cd779e873306675c7a58e86d6d532 # v2
        with:
          tool: sccache
          version: 0.7.5

      - name: Configure sccache backend
        if: ${{ env.USE_SCCACHE == 'true' }}
        shell: bash
        run: |
          set -euo pipefail
          if [[ -n "${ACTIONS_CACHE_URL:-}" && -n "${ACTIONS_RUNTIME_TOKEN:-}" ]]; then
            echo "SCCACHE_GHA_ENABLED=true" >> "$GITHUB_ENV"
            echo "Using sccache GitHub backend"
          else
            echo "SCCACHE_GHA_ENABLED=false" >> "$GITHUB_ENV"
            echo "SCCACHE_DIR=${{ github.workspace }}/.sccache" >> "$GITHUB_ENV"
            echo "Using sccache local disk + actions/cache fallback"
          fi

      - name: Enable sccache wrapper
        if: ${{ env.USE_SCCACHE == 'true' }}
        shell: bash
        run: echo "RUSTC_WRAPPER=sccache" >> "$GITHUB_ENV"

      - name: Restore sccache cache (fallback)
        if: ${{ env.USE_SCCACHE == 'true' && env.SCCACHE_GHA_ENABLED != 'true' }}
        id: cache_sccache_restore
        uses: actions/cache/restore@v5
        with:
          path: ${{ github.workspace }}/.sccache/
          key: sccache-${{ matrix.runner }}-${{ matrix.target }}-${{ matrix.profile }}-${{ steps.lockhash.outputs.hash }}-${{ github.run_id }}
          restore-keys: |
            sccache-${{ matrix.runner }}-${{ matrix.target }}-${{ matrix.profile }}-${{ steps.lockhash.outputs.hash }}-
            sccache-${{ matrix.runner }}-${{ matrix.target }}-${{ matrix.profile }}-

      - uses: taiki-e/install-action@44c6d64aa62cd779e873306675c7a58e86d6d532 # v2
        with:
          tool: nextest
          version: 0.9.103

      - if: ${{ contains(matrix.target, 'aarch64-unknown-linux') }}
        name: Cap build parallelism (ARM)
        shell: bash
        run: echo "CARGO_BUILD_JOBS=4" >> "$GITHUB_ENV"

      - name: Enable unprivileged user namespaces (Linux)
        if: runner.os == 'Linux'
        run: |
          # Required for bubblewrap to work on Linux CI runners.
          sudo sysctl -w kernel.unprivileged_userns_clone=1
          # Ubuntu 24.04+ can additionally gate unprivileged user namespaces
          # behind AppArmor.
          if sudo sysctl -a 2>/dev/null | grep -q '^kernel.apparmor_restrict_unprivileged_userns'; then
            sudo sysctl -w kernel.apparmor_restrict_unprivileged_userns=0
          fi

      - name: Reduce Windows test debug info
        if: runner.os == 'Windows'
        shell: bash
        run: |
          set -euo pipefail
          # Avoid sporadic LINK LNK1318 PDB corruption failures when nextest
          # links many Windows test binaries in parallel.
          echo "CARGO_PROFILE_CI_TEST_DEBUG=0" >> "$GITHUB_ENV"

      - name: Configure rusty_v8 artifact overrides
        # v8 v147+ defaults to the `ptrcomp_sandbox` build variant, which
        # `denoland/rusty_v8` does not ship for non-Windows targets. Point
        # the v8 build script at the upstream Codex release for those
        # targets so the build doesn't 404 against denoland.
        # `GN_ARGS=use_custom_libcxx=false` makes the v8 build script emit
        # the libstdc++/libc++ link directive since the prebuilt
        # ptrcomp_sandbox archives do not bundle libcxx.
        #
        # On Windows MSVC, the openai/codex `ptrcomp_sandbox` Windows
        # prebuilt is published as a Unix `.a.gz` archive whose bundled
        # `binding_147_4_0.pic.o` triggers `LNK1143: invalid or corrupt
        # file: no symbol for COMDAT section` when MSVC `link.exe` consumes
        # it. denoland's regular `release` variant for Windows is published
        # as a proper `.lib.gz` (COFF) archive, so use that instead — it
        # doesn't have `v8_enable_sandbox` set, but neither does anything
        # in upstream codex CI on Windows.
        env:
          TARGET: ${{ matrix.target }}
        shell: bash
        run: |
          set -euo pipefail
          version="$(python3 "${GITHUB_WORKSPACE}/.github/scripts/rusty_v8_bazel.py" resolved-v8-crate-version)"
          release_tag="rusty-v8-v${version}"
          if [[ "${TARGET}" == *-pc-windows-msvc ]]; then
            base_url="https://github.com/denoland/rusty_v8/releases/download/v${version}"
            archive_name="rusty_v8_release_${TARGET}.lib.gz"
            binding_name="src_binding_release_${TARGET}.rs"
          else
            base_url="https://github.com/openai/codex/releases/download/${release_tag}"
            archive_name="librusty_v8_ptrcomp_sandbox_release_${TARGET}.a.gz"
            binding_name="src_binding_ptrcomp_sandbox_release_${TARGET}.rs"
          fi
          archive="${base_url}/${archive_name}"
          binding_dir="${RUNNER_TEMP}/rusty_v8"
          binding_path="${binding_dir}/${binding_name}"
          mkdir -p "${binding_dir}"
          curl -fsSL "${base_url}/${binding_name}" -o "${binding_path}"
          echo "RUSTY_V8_ARCHIVE=${archive}" >> "$GITHUB_ENV"
          echo "RUSTY_V8_SRC_BINDING_PATH=${binding_path}" >> "$GITHUB_ENV"
          echo "GN_ARGS=use_custom_libcxx=false" >> "$GITHUB_ENV"
          # The upstream Codex release `ptrcomp_sandbox_release` archive is built against
          # libc++ (`std::__1::*` symbols) on every platform, so route the v8
          # build script's link directive to libc++ instead of the per-target
          # default (libstdc++ on linux-gnu).
          case "${TARGET}" in
            *-linux-gnu|*-linux-musl) echo "CXXSTDLIB=c++" >> "$GITHUB_ENV" ;;
          esac

      - name: tests
        id: test
        # On Windows, exclude `codex-v8-poc` and `codex-code-mode` from the
        # workspace test set so `--all-features` does NOT activate their
        # `sandbox = ["v8/v8_enable_sandbox"]` feature. The denoland v8
        # release variant we use on Windows is built without sandbox, so
        # enabling the Cargo feature would mismatch the archive ABI. The
        # crates are still compiled as deps of `codex-core` (with default
        # features), so production code paths are still exercised by the
        # other test crates.
        run: |
          if [ "${{ runner.os }}" = "Windows" ]; then
            cargo nextest run --all-features --no-fail-fast --target ${{ matrix.target }} --cargo-profile ci-test --timings --workspace --exclude codex-v8-poc --exclude codex-code-mode
          else
            cargo nextest run --all-features --no-fail-fast --target ${{ matrix.target }} --cargo-profile ci-test --timings
          fi
        shell: bash
        env:
          RUST_BACKTRACE: 1
          NEXTEST_STATUS_LEVEL: leak

      - name: Upload Cargo timings (nextest)
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: cargo-timings-ata-nextest-${{ matrix.target }}-${{ matrix.profile }}
          path: codex-rs/target/**/cargo-timings/cargo-timing.html
          if-no-files-found: warn

      - name: Save cargo home cache
        if: always() && !cancelled() && steps.cache_cargo_home_restore.outputs.cache-hit != 'true'
        continue-on-error: true
        uses: actions/cache/save@v5
        with:
          path: |
            ~/.cargo/bin/
            ~/.cargo/registry/index/
            ~/.cargo/registry/cache/
            ~/.cargo/git/db/
          key: cargo-home-${{ matrix.runner }}-${{ matrix.target }}-${{ matrix.profile }}-${{ steps.lockhash.outputs.hash }}-${{ steps.lockhash.outputs.toolchain_hash }}

      - name: Save sccache cache (fallback)
        if: always() && !cancelled() && env.USE_SCCACHE == 'true' && env.SCCACHE_GHA_ENABLED != 'true'
        continue-on-error: true
        uses: actions/cache/save@v5
        with:
          path: ${{ github.workspace }}/.sccache/
          key: sccache-${{ matrix.runner }}-${{ matrix.target }}-${{ matrix.profile }}-${{ steps.lockhash.outputs.hash }}-${{ github.run_id }}

      - name: sccache stats
        if: always() && env.USE_SCCACHE == 'true'
        continue-on-error: true
        working-directory: .
        run: sccache --show-stats || true

      - name: sccache summary
        if: always() && env.USE_SCCACHE == 'true'
        shell: bash
        working-directory: .
        run: |
          {
            echo "### sccache stats — ${{ matrix.target }} (tests)";
            echo;
            echo '```';
            sccache --show-stats || true;
            echo '```';
          } >> "$GITHUB_STEP_SUMMARY"

      - name: verify tests passed
        if: steps.test.outcome == 'failure'
        run: |
          echo "Tests failed. See logs for details."
          exit 1

  # --- Gatherer job ----------------------------------------------------------
  results:
    name: Platform CI results
    needs: [changed, lint_build, tests]
    if: always()
    runs-on: ubuntu-24.04
    steps:
      - name: Summarize
        shell: bash
        run: |
          echo "lint   : ${{ needs.lint_build.result }}"
          echo "tests  : ${{ needs.tests.result }}"

          # Draft PRs intentionally skip expensive jobs.
          if [[ '${{ github.event.pull_request.draft }}' == 'true' ]]; then
            echo 'Draft PR -> expensive CI skipped.'
            exit 0
          fi

          # If nothing relevant changed, declare success.
          if [[ '${{ needs.changed.outputs.codex }}' != 'true' && '${{ needs.changed.outputs.workflows }}' != 'true' && '${{ github.event_name }}' != 'push' ]]; then
            echo 'No relevant changes -> CI not required.'
            exit 0
          fi

          [[ '${{ needs.lint_build.result }}' == 'success' ]] || { echo 'lint_build failed'; exit 1; }
          [[ '${{ needs.tests.result }}' == 'success' ]] || { echo 'tests failed'; exit 1; }

      - name: sccache summary note
        if: always()
        run: |
          echo "Per-job sccache stats are attached to each matrix job's Step Summary."
